version: '3.5'

services:
  authorization-app:
    build: ./
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      - HOST="localhost"
      - USER="dolganoffadmin"
      - PASSWORD="dolganoffadmin"
      - DBNAME="users"
      - PORT="5436"
      - SECRET_ACCESS="jellybellybell"
      - SECRET_REFRESH="freshmeat"
      - CLIENT_SECRET="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"
      - CLIENT_ID="891267034828-iaqbrrn62te9ps06esnl8j7fnqk2uok2.apps.googleusercontent.com"
      - REDIRECT_URL="http://localhost:5
      - 000/api/auth/google-callback"
      - USERINFO_EMAIL="https://www.googleapis.com/auth/userinfo.email"
      - USERINFO_PROFILE="https://www.googleapis.com/auth/userinfo.profile"
      - GOOGLE_APIS="https://www.googleapis.com/oauth2/v2/userinfo?access_token="
  db:
    restart: always
    image: postgres
    volumes:
      - ./db/postgres/data:/var/lib/postgresql/data
    environment:
    - HOST="localhost"
    - USER="dolganoffadmin"
    - PASSWORD="dolganoffadmin"
    - DBNAME="users"
    - PORT="5436"
    ports:
      - "5436:5436"
